<h1>Livros disponíveis</h1>
<div class="books-container">
    {{#each books}}
        <div>#{{this.id}} - <span class="bold"><a href="/books/{{this.id}}">{{this.title}}</a></span> 
        - {{this.pageqty}} paginas <div id="editar_id"><a href="/books/edit/{{this.id}}">Editar</a></div>
        </div>
    {{/each}}
</div>